/**
 * Configuration flag for Cloud Synchronization (Firebase Auth + Firestore).
 * 
 * Set to `false` for local offline/demo mode (pitch presentation, tests, standalone browser demo).
 * Set to `true` to enable real-time Firebase Authentication and Firestore database sync.
 */
export const CLOUD_SYNC_ENABLED = false;
